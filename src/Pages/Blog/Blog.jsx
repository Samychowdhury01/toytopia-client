import React from 'react';
import useTitle from '../../Hooks/useTitle';
import Header from '../Shared/Header/Header';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            <Header title={'Blog'}/>
            <div>
                {
                    
                }
            </div>
        </div>
    );
};

export default Blog;